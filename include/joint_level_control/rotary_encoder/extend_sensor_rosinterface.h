#ifndef EXTEND_SENSOR_ROS
#define EXTEND_SENSOR_ROS

#define EXTEND_SENSOR_ID 3
// reads angle from the extend sensor of the leg
// returns the angle in radians (i think)
double readExtendAngle(int sensorID);

#endif